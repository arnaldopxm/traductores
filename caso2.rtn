program
  with
    number x = 0;
    boolean y = true;
    number i = 10;
  do
    while y do
      if i > 0 then
        writeln i;
        i = i - 1;
      else
        writeln "Programa Terminado";
        y = false;
      end;
    end;
    if then end;
    # x = 10;
    # x = 200/10 % 3;
    # y = true and false;
    # y = (5 <= 5) and not (true and false);
    #
    # if true and false then
    #   x = 1;
    # else
    #   x = x + 1;
    # end;

  end;
end;
